type Justification = {
    uid?: string,
    user_uid: string,
    title: string,
    description?: string,
    date: Date,
    amount: Float32Array,
    employee_uid: string,
}

export default Justification;